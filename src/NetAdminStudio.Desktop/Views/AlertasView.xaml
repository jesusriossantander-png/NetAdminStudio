<UserControl x:Class="NetAdminStudio.Desktop.Views.AlertasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="18">
        <DockPanel Margin="6,4,6,0">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Alertas e Incidentes" FontSize="26" FontWeight="SemiBold"/>
                <TextBlock Text="Eventos que requieren atención"
                           Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>
            <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButton}"
                    Content="↻  Actualizar" Command="{Binding RefreshCommand}"
                    VerticalAlignment="Center"/>
        </DockPanel>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="Alertas abiertas" FontSize="16" FontWeight="SemiBold"/>
                </DockPanel>

                <TextBlock Text="No hay alertas abiertas. Todo en orden. ✅"
                           Foreground="{StaticResource MutedBrush}" Margin="2,6"
                           Visibility="{Binding Alerts.Count, Converter={StaticResource ZeroToVisibility}}"/>

                <ItemsControl ItemsSource="{Binding Alerts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceAltBrush}"
                                    CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                <DockPanel>
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <Button Content="Reconocer"
                                                Command="{Binding DataContext.AcknowledgeAlertCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding Id}"/>
                                        <Button Content="Resolver" Style="{StaticResource PrimaryButton}"
                                                Command="{Binding DataContext.ResolveAlertCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding Id}"/>
                                    </StackPanel>

                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                                                   TextWrapping="Wrap"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="6"
                                                    Padding="8,2" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding SeverityText}" FontSize="11"/>
                                            </Border>
                                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="6"
                                                    Padding="8,2" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding StateText}" FontSize="11"
                                                           Foreground="{StaticResource AccentBrush}"/>
                                            </Border>
                                            <TextBlock Text="{Binding SourceType}" FontSize="11"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding OpenedText}" FontSize="11"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
